# fn-knock 文档国际化架构

站点采用 VitePress 内置 i18n：简体中文位于根路径，其他语言使用固定前缀。所有语言共享相同的逻辑页面路径，因此语言切换器可以在当前页面之间直接跳转。

## 目录结构

```text
docs/
├── index.md                         # zh-CN: /
├── quick-start/                    # zh-CN: /quick-start/*
├── tutorials/                      # zh-CN: /tutorials/*
├── guide/                          # zh-CN: /guide/*
├── origin/                         # zh-CN: /origin/*
├── faq.md                          # zh-CN: /faq
├── zh-tw/
│   ├── index.md                    # zh-TW: /zh-tw/
│   ├── quick-start/
│   ├── tutorials/
│   ├── guide/
│   ├── origin/
│   └── faq.md
├── en/                             # en-US: /en/*
├── ja/                             # ja-JP: /ja/*
├── ko/                             # ko-KR: /ko/*
└── .vitepress/
    ├── config.ts                   # 站点 locales 映射与共享配置
    ├── i18n/
    │   ├── catalog.ts              # 语言元数据、术语、页面标题、nav/sidebar
    │   └── paths.ts                # 动态列表与动态路由的路径工具
    └── theme/
        └── locale-preference.ts    # 浏览器语言匹配与用户偏好持久化
```

四个前缀目录与根目录保持严格的一一对应关系。`npm run check:i18n` 会检查缺页、多余页面、页面标题和错误的跨语言链接。

## Locale 契约

| 配置键 | HTML `lang` | 路径 | 语言菜单标签 |
| --- | --- | --- | --- |
| `root` | `zh-CN` | `/` | 简体中文 |
| `zh-tw` | `zh-TW` | `/zh-tw/` | 繁體中文（台灣） |
| `en` | `en-US` | `/en/` | English |
| `ja` | `ja-JP` | `/ja/` | 日本語 |
| `ko` | `ko-KR` | `/ko/` | 한국어 |

每个 locale 都在 `config.ts` 顶层 `locales` 中声明 `label`、`lang`、`link`、`title`、`description` 和独立的 `themeConfig`。共享 Logo、Algolia、社交链接等放在顶层 `themeConfig`，避免在 locale 之间重复。

构建时会为每个页面生成五个 `hreflang` alternate 链接以及指向简体中文的 `x-default`，页面的 `<html lang>`、标题、导航、侧栏、上一篇/下一篇、更新时间、404 和页尾入口均随 locale 变化。

## 浏览器语言自适应

只在访客首次打开根首页 `/` 时读取 `navigator.languages`：

1. `zh-TW`、`zh-Hant`、`zh-HK`、`zh-MO` 匹配 `/zh-tw/`。
2. `en-*`、`ja-*`、`ko-*` 分别匹配 `/en/`、`/ja/`、`/ko/`。
3. `zh-CN`、其他简体中文标签、未知语言或 API 不可用时保留 `/`，即回退到简体中文。
4. 访问深层链接时不自动改写语言，避免破坏分享链接、书签和搜索引擎入口。
5. 用户通过导航栏切换语言后，选择写入 `fn-knock-docs-locale`，以后访问根首页优先使用该选择。

## 内容同步

简体中文是源内容。新增文章时：

```bash
npm run i18n:sync
```

命令会补齐缺失页面，并更新仍标记为 `translationStatus: source-synced` 的页面；已经进入人工翻译流程的页面不会被覆盖。同步页会：

- 写入目标 `lang`、本地化页面标题和 `sourceLocale: zh-CN`；
- 将站内文档链接改为当前语言前缀；
- 保留相同文件名和目录层级，保证路由契约稳定。

完成正文翻译后，可将 frontmatter 的 `translationStatus` 从 `source-synced` 改为团队采用的发布状态。标题、导航和侧栏术语集中维护在 `docs/.vitepress/i18n/catalog.ts`。

## HomeLab / NAS 术语基线

术语按当地自托管社区的常见说法维护，不做逐字转换：

| 简体中文 | 繁體中文（台灣） | English | 日本語 | 한국어 |
| --- | --- | --- | --- | --- |
| Docker 容器 | Docker 容器 / Container | Docker Containers | Docker コンテナ | Docker 컨테이너 |
| 硬件加速 / 硬解 | 硬體加速 / 硬解 | Hardware Acceleration | ハードウェアアクセラレーション | 하드웨어 가속 |
| 网络存储 | 網路儲存 | Network-Attached Storage | ネットワークストレージ | 네트워크 스토리지 |
| 共享文件夹 | 分享資料夾 | Shared Folder | 共有フォルダー | 공유 폴더 |
| 内网穿透 | 內網穿透 / NAT Traversal | NAT Traversal / Tunneling | NAT 越え / トンネル | NAT 통과 / 터널링 |
| 反向代理 | 反向代理 | Reverse Proxy | リバースプロキシ | 역방향 프록시 |
| 公网 IP | 公網 IP | Public IP Address | 公開 IP アドレス | 공인 IP 주소 |
| 子域映射 | 子網域路由 | Subdomain Routing | サブドメインルーティング | 서브도메인 라우팅 |
| IP 白名单 | IP 允許清單 | IP Allowlist | IP 許可リスト | IP 허용 목록 |
| 群晖 | Synology | Synology DSM | Synology DSM | Synology DSM |
| 黑群晖 | 非官方 Synology DSM / 黑群暉 | Unofficial Xpenology build | 非公式 Xpenology 環境 | 비공식 Xpenology 빌드 |
| 玩客云 | OneCloud 玩客雲 | OneCloud device | OneCloud デバイス | OneCloud 장치 |

产品名、协议名、界面字段和命令保持原文；涉及 `Host`、TLS、OIDC、TOTP、Passkey、FRP、Cloudflare Tunnel 等概念时，优先使用社区约定的技术名。

## 动态文章列表与 `.paths.ts`

当前项目没有 `.data.ts` 或 `.paths.ts`。后续添加动态列表时，必须从 `docs/.vitepress/i18n/paths.ts` 导入路径函数，不要手写前缀：

```ts
import { createContentLoader } from 'vitepress'
import { fromLocalePath } from './.vitepress/i18n/paths'

export default createContentLoader('**/*.md', {
  excerpt: true,
  transform(pages) {
    return pages.map((page) => ({
      ...page,
      ...fromLocalePath(page.url),
    }))
  },
})
```

页面组件根据当前 locale 筛选列表。若数据源是 CMS 或 JSON，可一次扩展五套稳定 URL：

```ts
import { expandLocalizedEntries } from './.vitepress/i18n/paths'

const localizedPosts = expandLocalizedEntries(
  posts,
  (post) => `/posts/${post.slug}`,
)
```

根语言 URL 仍为 `/posts/example`，其余语言分别为 `/zh-tw/posts/example`、`/en/posts/example`、`/ja/posts/example` 和 `/ko/posts/example`。动态路由的 `paths()` 应复用同一数据集，并为根模板与前缀模板分别筛选 `locale`，从而保持静态文档相同的一一对应契约。
