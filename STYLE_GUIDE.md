# fn-knock 文档写作规范

这份规范约束 `docs/` 下的用户文档。文档以已发布产品的实际行为为准；源码、后台界面和部署脚本不一致时，先确认行为，再改文档。多语言目录、路径规则和术语基线同时遵循 [国际化架构说明](./I18N.md)。

## 从用户任务出发

先回答读者正在完成的任务，再解释实现细节。产品的基本心智模型分为三层：

1. **网络拓扑**：公网直连，或通过 FRP、Cloudflared 等隧道进入。
2. **路由方式**：按 Host、按路径，或转发 TCP / UDP。
3. **访问策略**：公开访问、要求登录，以及登录凭据的服务范围。

上游 Basic Auth 凭据注入是代理到目标服务时的连接选项，不是 fn-knock 对访问者提供的登录方式。

当前 Host 编辑界面只提供“要求登录”开关。历史配置中可能仍有严格白名单字段；只在迁移或排障时解释其行为，不把它写成新建映射时可选择的控件。

手动来源授权可以独立放行 Host；登录后自动 IP 授权通常用于同一来源的继续访问，但不能覆盖浏览器中已携带的服务范围拒绝。不要把“要求登录”写成只认浏览器会话。历史严格白名单按有效来源授权记录判断，浏览器 Cookie 本身不能替代来源条件，也不应假定关闭“要求登录”就会把它变成公开映射。

服务范围受限的登录凭据不会创建可放行任意 Host 或原始端口的通用自动 IP 授权；直连模式需使用未限制范围的凭据或手动 IP / CIDR。协议映射可以作为自定义范围项单独选择：登录后 IP 授权未关闭时，只为所选 `TCP/UDP + 对外端口` 创建来源 IP 授权，不应写成必须使用未限制范围的凭据。

后台的“直连模式”“内网穿透”“子域模式”是这些选择的组合，不应成为文档唯一的解释框架。

## 用词

- 产品名统一写作 `fn-knock`。需要对应飞牛桌面名称时，写“敲门 knock”。
- 使用后台中的真实名称，例如 `系统设置 → 模式`、`子域映射`、`IP 白名单`。
- “管理入口”指管理面板；“网关入口”指用户访问服务时经过的入口。不要混称“后台端口”。
- 示例统一使用 `example.com`、`auth.example.com`、`nas.example.com` 和文档保留地址。
- 第一次出现缩写时解释一次；后文不重复下定义。

## 语气

- 直接陈述事实或操作，不用“这页解决什么”“先记住”“到底”“真实含义”等讲稿式铺垫。
- 不用“先进”“一站式”“彻底挡住”“绝对安全”等无法验证的宣传语。
- 少用第二人称和反问句。能写成步骤、条件或结果时，不写成劝说。
- 只在存在明确取舍时给建议，并写清适用条件。
- 一段只表达一个意思；能删掉而不损失信息的句子应删除。

## 页面结构

根据内容选用必要部分，不强行套模板：

- 开头两段内说明功能、适用条件和关键限制。
- 操作页按真实界面顺序给步骤。
- 复杂字段用表格；简单字段用短列表。
- 给出可观察的验证结果，而不是只写“保存成功”。
- 排错按链路顺序排列，并告诉读者应该看哪个状态或日志。
- 相关链接保留 2～5 个，不在每页复制整套导航。

## 安全与版本边界

- `fn-knock` 用于收敛入口和前置认证，不能替代系统更新、备份、最小权限和上游服务自身的安全配置。
- 不把局域网测试结果当作公网认证结果；网关默认信任私网或本地来源，公网验证应使用移动网络等真实外部链路。
- Docker、飞牛 FPK、OpenWrt、Linux、macOS、群晖 DSM 和 Windows 的能力不同。涉及防火墙、直连模式、内置隧道、Web 终端、智能连接或应用内更新时，必须写明部署限制。macOS 文档必须明确说明不支持 `iptables` 或主机防火墙管理。
- 未形成公开下载、升级和回滚闭环的功能不写进用户安装路径。
- 备份和凭据导出都按敏感文件处理。不要把归档加密描述成可替代安全存储的保护措施。

## 发布前检查

1. 对照后台界面确认菜单、字段和默认值。
2. 对照部署文件确认端口、卷、命令和能力限制。
3. 用真实外部网络完成关键链路验证。
4. 运行 `npm run check:docs` 和 `npm run build`，修复失效链接、禁用表达和 VitePress 构建错误。
5. 搜索旧术语、重复段落和占位示例。
6. 运行 `npm run check:i18n`，确认五种语言的页面路径、标题和站内链接一一对应。
